apiVersion: v1
kind: Service
metadata:
  name: {{ include "pfe-python-app.fullname" . }}
  labels:
    {{- include "pfe-python-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pfe-python-app.selectorLabels" . | nindent 4 }}
